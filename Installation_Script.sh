#! /usr/bin/env bash

# Check if python is installed, sys.path would give this in the python int

# Install pip3 for python3 if not already installed
sudo apt-get install python3-pip

# Install requests
sudo pip3 install requests

# Install pytube
sudo pip3 install pytube

echo "Installation successful. You may now run the project."
